<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to "#{t("web-app-theme.list", :default => t("views.links.list"))}", entadmin_users_path  %></li>
    </ul>
  </div>          
  <div class="content">    
  	<br>         
    <div class="inner">
      <% form_for :user, :url => entadmin_users_path(:empid => @empid, :form => @form), :html => { :class => :form } do |f| -%>
        <%= render :partial => @which_form, :locals => {:f => f} %>
      <% end -%>
    </div>
  </div>
</div>
