<div class="block">
	<div class="secondary-navigation">
		<ul class="wat-cf">
			<li class="first active">
				<%= link_to "#{t("web-app-theme.list", :default => t("views.links.list"))}", sysadmin_publicnumbers_path %>
			</li>
			<li class="first">
				<%= link_to "#{t("web-app-theme.list", :default => t("views.links.add"))}", new_sysadmin_publicnumber_path %>
		    <li class="first">
				<%= link_to "#{t("web-app-theme.list", :default => t("views.links.assign_unassign"))}", assign_unassign_sysadmin_publicnumbers_path %>
			</li>
		</ul>
	</div>
	<div class="content" >
		<br>
		<div class="inner" style="width:50%">
			<table id="sortableTable" class="tablesorter">
				<thead>
					<tr>
						<th><%= t("views.titles.publicnumbers")%></th>
						<th class="last" style="text-align: left"> <%= t("views.titles.action")%> </th>
					</tr>
				</thead>
				<tbody>
					<% row = 0%>
					<% @page_results.each do |public_number| -%>
					<tr class="<%= cycle("odd", "even") %>">
						<td align="left"> <%= public_number["number"] %></td>
						<td class="last"> <%= link_to "#{t("web-app-theme.edit", :default => t("views.links.delete"))}",  sysadmin_publicnumber_path(public_number["number"]), :method => "delete", :confirm => t("views.notices.ippbx_delete_publicnumber")%></td>
					</tr>
					<% row += 1%>
					<% end %>
				</tbody>
			</table>
<% if @page_results.total_pages > 1 %>
<div style="height:30px;">
	<%= will_paginate @page_results %>
</div>
<% end %>
		</div>
	</div>